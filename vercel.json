{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/admin", "destination": "/api/admin" }
  ],
  "functions": {
    "api/**/*.js": {
      "includeFiles": "server/admin.html",
      "maxDuration": 10
    }
  }
}
